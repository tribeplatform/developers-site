---
id: custom-field-validators
title: CustomFieldValidators
---

No description

```graphql
enum CustomFieldValidators {
  allOf
  anyOf
  enum
  exclusiveMaximum
  exclusiveMinimum
  format
  maxItems
  maxLength
  maxProperties
  maximum
  minItems
  minLength
  minProperties
  minimum
  multipleOf
  not
  oneOf
  pattern
  uniqueItems
}
```

### Values

#### `allOf`

#### `anyOf`

#### `enum`

#### `exclusiveMaximum`

#### `exclusiveMinimum`

#### `format`

#### `maxItems`

#### `maxLength`

#### `maxProperties`

#### `maximum`

#### `minItems`

#### `minLength`

#### `minProperties`

#### `minimum`

#### `multipleOf`

#### `not`

#### `oneOf`

#### `pattern`

#### `uniqueItems`
